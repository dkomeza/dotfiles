# dotfiles

Managed with [chezmoi](https://chezmoi.io).

New machine:

    brew install chezmoi
    chezmoi init --apply dkomeza

Secrets live in `~/.zshrc.local` (not tracked): `export NPM_TOKEN=...`.
